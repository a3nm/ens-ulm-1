#include <vector>
#include <cstdio>
#include <algorithm>
#include <map>
#include <time.h>

using namespace std;

#define MAXN 200

int R, C, H, S;
char pizz[MAXN][MAXN]; // row, col

vector<int> size;
vector<vector<int> > G;
vector<int> r1;
vector<int> c1;
vector<int> r2;
vector<int> c2;
vector<int> ham;

int main(int argc, char **argv) {
  scanf("%d%d%d%d", &R, &C, &H, &S);
  
  return 0;
}


